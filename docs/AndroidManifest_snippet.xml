<!-- 
  Добавете този код във вашия AndroidManifest.xml
  Поставете го вътре в <application> тага
-->

<activity
    android:name=".ProvisioningCompleteActivity"
    android:exported="true"
    android:launchMode="singleTop"
    android:theme="@style/Theme.AppCompat.Light.NoActionBar">
    
    <!-- Intent filter за provisioning complete -->
    <intent-filter>
        <action android:name="android.app.action.PROVISIONING_SUCCESSFUL" />
        <category android:name="android.intent.category.DEFAULT" />
    </intent-filter>
</activity>

<!-- Permissions нужни за provisioning -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.WRITE_SETTINGS" />
<uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
<uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />

<!-- TODO: Ако инсталирате APK-та програмно -->
<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
